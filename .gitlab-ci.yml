# ───────────────────────| Git Sentinel CI/CD pipeline |────────────────────────
#
# STAGES
#
#   - .pre       (implicit) checks whether main has moved since the last
#                 nightly build
#   - test       mypy strict + pytest (runs on every pipeline)
#   - build      compile the Nuitka binary
#   - publish    upload the binary to the Generic Package Registry
#   - release    create a GitLab Release (tagged builds only)
#
#
# TRIGGERS
#
#   MR / Commit on main:
#     -> test       (Linux + Windows)
#     -> build      (Linux + Windows)
#
#   Versioned tag:
#     -> test       (Linux + Windows)
#     -> build      (Linux + Windows)
#     -> publish    (Linux + Windows)
#     -> release
#
#   Scheduled pipeline (main):
#     -> check:nightly    compares $CI_COMMIT_SHA against the SHA recorded by
#                          the last successful nightly publish and generates
#                          generated-nightly.yml: a copy of
#                          .gitlab/ci/nightly.yml (the real test/build/publish
#                          chain) if main has moved, or a single no-op job if
#                          not. Always succeeds - it never fails on purpose,
#                          it just decides what the next job runs.
#     -> trigger:nightly  runs generated-nightly.yml as a child pipeline
#                          (strategy: depend, so this job's own status
#                          mirrors it). On an unchanged main that's just the
#                          no-op job, so the whole test/build/publish suite
#                          is skipped - not failed. See .gitlab/ci/nightly.yml
#                          for the real nightly chain.
#
#
# COMMIT TYPE FILTER
#
#   Skipped entirely:
#   - docs      No code changes; nothing to test, build, or release.
#   - chore     Maintenance tasks (deps, config, tooling) with no shippable
#                output.
#   - style     [FUTURE] Formatting only; will trigger lint once a linter is
#                introduced. Skipped entirely for now as linting requires a
#                CI-authored commit.
#
#   Partially skipped (test only; no build, publish, or release):
#   - test      Test changes only; code itself is unchanged.
#
#   Partially skipped    |-> test -> build -|
#   - ci        Pipeline config changes; build runs to validate the config but
#                no shippable output (publish/release are tag/schedule gated).
#
#   Partially skipped    |-> test -> build -|
#   - build     Dependency or toolchain changes; not a shippable feature or fix.
#
#   Full pipeline        |-> test -> build -> publish -> release --|
#   - feat      Minor version bump.
#   - fix       Patch version bump.
#   - perf      Patch version bump.
#   - refactor  Patch version bump.
#   - revert    Patch version bump.
#   - BREAKING  Major version bump.
#
#
# NIGHTLY (setup):
#
#   Go to CI/CD > Schedules in GitLab and create a schedule targeting the main
#   branch (e.g. daily at 03:00 UTC).  Each run overwrites the single "nightly"
#   package entry so the download URL stays stable.
#
#   Nightly download URLs (once a schedule has run):
#   - $CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/git-sentinel/nightly/git-sentinel
#   - $CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/git-sentinel/nightly/git-sentinel-gui
#   - $CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/git-sentinel/nightly/git-sentinel.exe
#   - $CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/git-sentinel/nightly/git-sentinel-gui.exe
#
#   Change detection: check:nightly downloads
#   .../packages/generic/git-sentinel/nightly/nightly-sha.txt (the commit SHA
#   recorded by the last successful nightly publish) and compares it to
#   $CI_COMMIT_SHA. A match means main hasn't moved since the last nightly, so
#   the generated child pipeline is a single no-op job instead of the real
#   test/build/publish chain. No file yet (first-ever run) is treated as
#   "always build".

# ──────────────────────────────────────────────────────────| Workflow Setup |──
#
#
# Prevents duplicate pipelines when pushing to a branch with an open MR. Without
# this, GitLab fires both a branch pipeline and an MR pipeline.
#
# auto_cancel on_new_commit: interruptible - when a new commit is pushed, cancel
# any running jobs marked `interruptible: true`.
# The "publish" and "release" jobs are explicitly marked `interruptible: false`
# so they are never cancelled mid-run.
workflow:
  auto_cancel:
    on_new_commit: interruptible
  rules:
      # nightly always runs
    - if: $CI_PIPELINE_SOURCE == "schedule"

      # release tags always run
    - if: $CI_COMMIT_TAG

      # skip docs/chore/style
    - if: $CI_COMMIT_MESSAGE =~ /^(docs|chore|style)(\(.+\))?!?:/
      when: never

      # MR pipelines only (suppresses branch pipeline)
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

      # pushes to main
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

# By default run all jobs on Linux and can be interrupted.
default:
  interruptible: true
  tags:
    - linux

stages:
  - test
  - build
  - publish
  - release

variables:
  BINARY:  git-sentinel
  PACKAGE: git-sentinel

# ───────────────────────────────────────────────────────| Nightly change gate |──
#
# Runs before everything else on a scheduled pipeline. Compares the current
# commit against the SHA recorded by the last successful nightly publish and
# generates the actual child pipeline config that trigger:nightly will run -
# it always exits 0. If main hasn't moved, generated-nightly.yml is a single
# no-op job; otherwise it's a copy of .gitlab/ci/nightly.yml (the real
# test/build/publish chain).
#
# This bakes the "anything to do?" decision directly into the generated YAML
# content rather than passing a variable for the child pipeline's own rules
# to read - GitLab does not reliably make a parent job's variables available
# when it evaluates a *child* pipeline's rules (gitlab-org/gitlab#408160), so
# a $NIGHTLY_HAS_CHANGES-style variable can silently read as unset there and
# make the gate look like it's stuck on "nothing changed".
check:nightly:
  stage: .pre
  image: curlimages/curl:latest
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
  script:
    - |
      rm -f nightly-sha.txt
      curl --fail --silent --header "JOB-TOKEN: $CI_JOB_TOKEN" \
           -o nightly-sha.txt \
           "$CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/$PACKAGE/nightly/nightly-sha.txt" \
           || true
      LAST_SHA="$(cat nightly-sha.txt 2>/dev/null || echo "")"
      if [ "$LAST_SHA" = "$CI_COMMIT_SHA" ]; then
        echo "No new commits since last nightly build ($LAST_SHA) - nothing to do."
        printf '%s\n' \
          'nightly:noop:' \
          '  tags:' \
          '    - linux' \
          '  image: alpine:latest' \
          '  script:' \
          '    - echo "No new commits since the last nightly build - nothing to do."' \
          > generated-nightly.yml
      else
        echo "New commits since last nightly build ($CI_COMMIT_SHA) - proceeding."
        cp .gitlab/ci/nightly.yml generated-nightly.yml
      fi
  artifacts:
    paths:
      - generated-nightly.yml

# Runs the config check:nightly generated. strategy: depend makes this job's
# own status mirror the child pipeline's status, so a real nightly failure
# (test/build/publish) still shows up as a failed pipeline here - only the
# "nothing to do" case stays green.
#
# forward.yaml_variables: false stops this pipeline's own YAML-defined
# variables (e.g. the global BINARY/PACKAGE above) from being forwarded into
# the child as trigger variables. Forwarded variables outrank a job's own
# `variables:` in the child (gitlab-org/gitlab#404772), so without this,
# BINARY: git-sentinel here silently overrides
# publish:windows:nightly's own BINARY: git-sentinel.exe override in
# .gitlab/ci/nightly.yml, and the Windows nightly publish uploads/looks for
# the wrong filename. The child pipeline defines everything it needs itself,
# so nothing from up here should leak in.
trigger:nightly:
  stage: test
  needs: [check:nightly]
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
  trigger:
    include:
      - artifact: generated-nightly.yml
        job: check:nightly
    strategy: depend
    forward:
      yaml_variables: false

# ────────────────────────────────────────────────────────────────────| Test |──
#
# mypy strict + pytest with a 75% coverage gate.
test:
  stage: test
  image: python:3.14
  rules:
      # handled by the nightly child pipeline instead
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - when: always
  before_script:
    - apt-get update -qq && apt-get install -y -qq xvfb tk
    - pip install uv --quiet
  script:
    - uv sync --frozen --group dev
    - uv run scripts/test.py
  coverage: '/^TOTAL\s+\d+\s+\d+\s+(\d+)%/'

# Uses Windows runner.
# Windows GUI tests are skipped entirely (not just deselected from coverage):
# Tkinter under uv's venv on Windows intermittently fails to find tk, causing
# flaky collection even on the same commit. --ignore=tests/unit/ui/gui keeps
# collection from ever importing tkinter on this runner.
test:windows:
  stage: test
  tags:
    - windows
  rules:
      # handled by the nightly child pipeline instead
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - when: always
  before_script:
    - pip install uv --quiet
  script:
    - uv sync --frozen --group dev
    - uv run scripts/test.py
  coverage: '/^TOTAL\s+\d+\s+\d+\s+(\d+)%/'

# ───────────────────────────────────────────────────| Shared build template |──

.build:
  stage: build
  image: python:3.14
  before_script:
    # patchelf is required by Nuitka's onefile/standalone mode on Linux.
    - apt-get update -qq && apt-get install -y -qq tk patchelf
    - pip install uv --quiet
  script:
    - bash scripts/build.sh
  artifacts:
    paths:
      - dist/git-sentinel
      - dist/git-sentinel-gui
    expire_in: 1 week

# ── Build jobs ────────────────────────────────────────────────────────────────

# Skipped on scheduled pipelines (build:nightly handles those) and on test:
# commits (no binary needed when only tests changed).
build:
  extends: .build
  rules:
      # not on schedule
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never

      # not when commit type is test
    - if: $CI_COMMIT_MESSAGE =~ /^test(\(.+\))?!?:/
      when: never

      # otherwise always
    - when: always

# Windows build. Nuitka auto-fetches a portable MinGW64 compiler on first
# use (--assume-yes-for-downloads in build.ps1 avoids an interactive prompt).
build:windows:
  stage: build
  tags:
    - windows
  before_script:
    - pip install uv --quiet
  script:
    - uv sync --frozen --group dev
    - pwsh scripts/build.ps1
  artifacts:
    paths:
      - dist/git-sentinel.exe
      - dist/git-sentinel-gui.exe
    expire_in: 1 week
  rules:
      # not on schedule
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never

      # not when commit type is test
    - if: $CI_COMMIT_MESSAGE =~ /^test(\(.+\))?!?:/
      when: never

      # otherwise always
    - when: always

# Nightly builds (Linux + Windows) live in .gitlab/ci/nightly.yml, triggered
# by trigger:nightly above.

# ─────────────────────────────────────────────────| Shared publish template |──

.publish:
  stage: publish
  image: curlimages/curl:latest
  interruptible: false    # never cancel a mid-flight upload
  script:
    - |
      curl --fail --silent --show-error \
           --header "JOB-TOKEN: $CI_JOB_TOKEN" \
           --upload-file "dist/$BINARY" \
           "$CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/$PACKAGE/$PACKAGE_VERSION/$BINARY"

# ────────────────────────────────────────────────────────────| Publish jobs |──

publish:release:
  extends: .publish
  needs: [build]
  variables:
    PACKAGE_VERSION: $CI_COMMIT_TAG
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/

publish:linux-gui:release:
  extends: .publish
  needs: [build]
  variables:
    PACKAGE_VERSION: $CI_COMMIT_TAG
    BINARY: git-sentinel-gui
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/

publish:windows:release:
  extends: .publish
  needs: [build:windows]
  variables:
    PACKAGE_VERSION: $CI_COMMIT_TAG
    BINARY: git-sentinel.exe
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/

publish:windows-gui:release:
  extends: .publish
  needs: [build:windows]
  variables:
    PACKAGE_VERSION: $CI_COMMIT_TAG
    BINARY: git-sentinel-gui.exe
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/

# publish:nightly, publish:windows:nightly, and publish:windows-gui:nightly
# live in .gitlab/ci/nightly.yml, triggered by trigger:nightly above.

# ─────────────────────────────────────────────────────────────────| Release |──

release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  interruptible: false    # never cancel a mid-flight release
  needs:
    - publish:release
    - publish:linux-gui:release
    - publish:windows:release
    - publish:windows-gui:release
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
  script:
    - |
      if echo "$CI_COMMIT_TAG" | grep -qE '[-.]alpha\.'; then
        CHANNEL="alpha"
      elif echo "$CI_COMMIT_TAG" | grep -qE '[-.]beta\.'; then
        CHANNEL="beta"
      else
        CHANNEL="stable"
      fi
      sh scripts/gen-release-notes.sh "$CI_COMMIT_TAG" "$CHANNEL" > release-notes.md
  release:
    name: "git-sentinel $CI_COMMIT_TAG"
    description: "./release-notes.md"
    tag_name: "$CI_COMMIT_TAG"
    assets:
      links:
        - name: "git-sentinel (Linux x86_64)"
          url: "$CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/$PACKAGE/$CI_COMMIT_TAG/git-sentinel"
          link_type: package
        - name: "git-sentinel-gui (Linux x86_64, windowed)"
          url: "$CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/$PACKAGE/$CI_COMMIT_TAG/git-sentinel-gui"
          link_type: package
        - name: "git-sentinel.exe (Windows x86_64)"
          url: "$CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/$PACKAGE/$CI_COMMIT_TAG/git-sentinel.exe"
          link_type: package
        - name: "git-sentinel-gui.exe (Windows x86_64, windowed)"
          url: "$CI_API_V4_URL/projects/$CI_PROJECT_ID/packages/generic/$PACKAGE/$CI_COMMIT_TAG/git-sentinel-gui.exe"
          link_type: package
